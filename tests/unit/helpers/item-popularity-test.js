
import { itemPopularity } from 'buddah-drop/helpers/item-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | item popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemPopularity([42]);
  assert.ok(result);
});

