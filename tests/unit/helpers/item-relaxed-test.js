
import { itemRelaxed } from 'buddah-drop/helpers/item-relaxed';
import { module, test } from 'qunit';

module('Unit | Helper | item relaxed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemRelaxed([42]);
  assert.ok(result);
});

