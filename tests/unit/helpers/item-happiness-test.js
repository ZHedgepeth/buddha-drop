
import { itemHappiness } from 'buddah-drop/helpers/item-happiness';
import { module, test } from 'qunit';

module('Unit | Helper | item happiness');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemHappiness([42]);
  assert.ok(result);
});

