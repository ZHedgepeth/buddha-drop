
import { itemPrice } from 'buddah-drop/helpers/item-price';
import { module, test } from 'qunit';

module('Unit | Helper | item price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemPrice([42]);
  assert.ok(result);
});

